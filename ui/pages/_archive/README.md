# Archived dashboard pages

Removed before the final presentation because each was redundant or empty in practice:

- `12_Reports.py` — showed "No reports yet"; the suggestion-quality reports it reads are not
  part of the refactoring loop.
- `04_Tool_Evidence.py` — a title and little else; tool evidence is shown in context on
  **Real Tool Loop** and **Presentation Summary**.
- `21_Refactoring_Verification.py` — superseded by **Real Tool Loop** (same before/after data,
  more detail) and **Presentation Summary**.

Nothing here is deleted; move a file back into `ui/pages/` to restore it.
